#include <bits/stdc++.h>
using namespace std;

void solve()
{
    // int n, k;
    // cin >> n >> k;

    // vector<int> days(n);
    // for (int i = 0; i < n; i++)
    // {
    //     cin >> days[i];
    // }

    // int peaks = 0;
    // int count = 0;
    // for (int i = 0; i <= n; i++)
    // {
    //     count = 0;
    //     for (int j = i; j < k + i && j < n; j++)
    //     {
    //         if (days[j] == 0)
    //         {
    //             count++;
    //         }
    //         else
    //             break;
    //     }

    //     if (count == k)
    //     {
    //         peaks++;
    //         i += k;
    //     }
    // }

    // cout << peaks << '\n';
}

int main()
{
    int t;
    cin >> t;

    while (t--)
    {
        solve();
    }

    return 0;
}